#Data Source
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=one_to_one_uni
spring.datasource.password=one_to_one_uni

#Turn off Spring banner
spring.main.banner-mode=off

#Set logging level
logging.level.root=WARN

#Logging configurations to display SQL statements generated by hibernate
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace

#Configuration below only for testing purpose only
#Configure to drop first and then auto create the tables by JPA/Hibernate.
#spring.jpa.hibernate.ddl-auto=create
#Auto create the tables only and it will keep data.
#spring.jpa.hibernate.ddl-auto=update
